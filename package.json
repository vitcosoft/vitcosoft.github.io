{
  "name": "vitcosoft.github.io",
  "version": "1.0.0",
  "description": "Hejka",
  "main": "scripts.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tailwindcss -i src/css/input.css -o build/css/output.css",
    "watch": "npx tailwindcss -i src/css/input.css -o build/css/output.css --watch"
  },
  "author": "Witold Wozniak",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}
